# Default port
port 6379

# Memory Management 
maxmemory 256mb
maxmemory-policy allkeys-lru

# Data Persistence Settings
appendonly yes
appendfilename "appendonly.aof"

# Network & Security
bind 0.0.0.0
protected-mode no